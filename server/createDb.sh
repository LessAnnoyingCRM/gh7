#!/usr/bin/env bash

sudo apt-get install mysql-server
#globalhacrm

#echo edited /etc/mysql/my.cnf to change bind-address to 0.0.0.0 (to enable remote access)
#CREATE USER gh7@'%' IDENTIFIED BY 'globalhacrm';
#GRANT ALL PRIVILEGES ON *.* TO 'gh7'@'%';
#FLUSH PRIVILEGES;

